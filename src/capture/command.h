#ifndef SHAULA_CAPTURE_COMMAND_H
#define SHAULA_CAPTURE_COMMAND_H

/* Executes one complete capture command and returns the public process code. */
int shaula_capture_command_run(int argc, char **argv);

#endif
