{
  "suite": "task-17-test-matrix",
  "required_layers": ["unit", "integration", "e2e_niri"],
  "required_cases": [
    "unit.preflight.schema",
    "unit.errors.matrix",
    "integration.capture.area",
    "integration.capture.fullscreen",
    "integration.pipeline.clipboard",
    "integration.daemon.states",
    "e2e.capture.area",
    "e2e.capture.fullscreen",
    "e2e.capture.window",
    "e2e.clipboard.path",
    "e2e.failure.unsupported_compositor",
    "e2e.failure.permission_clipboard",
    "e2e.backend.daemon_states"
  ],
  "required_error_token": "ERR_PREFLIGHT_ENV_NOT_READY"
}
